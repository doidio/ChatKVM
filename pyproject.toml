[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "chatkvm"
version = "0.1.0"
description = ""
requires-python = ">=3.13"
dependencies = [
    "mcp>=2.2.0",
    "ollama>=0.6.2",
    "pillow>=12.0.0",
    "streamlit>=1.63.0",
    "watchdog>=6.0.0",
]

[project.scripts]
chatkvm = "chatkvm.__main__:main"
